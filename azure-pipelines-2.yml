# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
  paths:
    include:
      - result/*

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: '261ab38b-35e9-4cbe-9633-d0a62605edb5'
  imageRepository: 'votingapp'
  containerRegistry: 'myvotingappcontainerregistry.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/result/Dockerfile'
  tag: '$(Build.BuildId)'

  # Agent VM image name
pool:
  name: votingAppAgent
  demands: Agent.Name -equals myVotingAppAgent

stages:
- stage: Build
  displayName: Build the Result Service
  jobs:
  - job: Build
    displayName: Build
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: 'myVotingAppContainerRegistry'
        repository: 'votingapp/result'
        command: 'build'
        dockerfilePath: 'result/Dockerfile'

- stage: Push
  displayName: Push the Result Service
  jobs:
    - job: Push
      displayName: Push
      steps:
      - task: Docker@2
        inputs:
          containerRegistry: 'myVotingAppContainerRegistry'
          repository: 'votingapp/result'
          command: 'push'

